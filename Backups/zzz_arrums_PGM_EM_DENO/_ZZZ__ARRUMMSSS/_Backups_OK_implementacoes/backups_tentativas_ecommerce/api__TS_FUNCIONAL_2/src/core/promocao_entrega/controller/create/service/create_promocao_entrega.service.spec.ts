// import { createPromocaoEntregaService } from '@src/core/promocao_entrega/controller/create/service/create_promocao_entrega.service'
// import { PromocaoEntregaRepositoryMemory } from '@src/core/promocao_entrega/repositories/memory/promocao_entrega.repository.memory'
// import { useSeedEntregaNotPromocao, useSeedEntregaMesmoDia, useSeedEntregaProximoDiaUtil } from '@src/core/promocao_entrega/_data/seed/promocao_entrega_unitarys_seed'

import { vi, describe, it, expect } from 'vitest'

describe.todo('[Service] createPromocaoEntregaService >> Sucess', () => {
    // vi.clearAllMocks()
    // const sut = createPromocaoEntregaService
    // const sutRepoMemory = new PromocaoEntregaRepositoryMemory()
    // const input = useSeedEntregaMesmoDia
    // vi.fn(createPromocaoEntregaService).mockImplementationOnce(async () => await sutRepoMemory.create(input))

    // it(`deve criar no service com mock >> os objetos promocao_entrega sempre via repo memory aqui no tester.`, async () => {
    //     const createdObjectByEntity = await sut(input)
    //     // console.log('CREATE IN SERVICE BY REPO_MEMORY >> ', createdObjectByEntity)

    //     expect(createdObjectByEntity).toHaveProperty('nomePromocao', 'entrega no mesmo dia')
    // })

    // it.todo(`deve ter o metodo "usePerformPromocaoEntrega" dentro via prototype do objeto`, async () => {
    //     const createdObjectByEntity1 = await sut(input)
    //     const response = createdObjectByEntity1.usePerformPromocaoEntrega

    //     expect(response).toBeTruthy()
    // })

    // it.todo(`deve criar os metodos corretamente nos prototype dos novos objetos.`, async () => {
    //     const input2 = useSeedEntregaProximoDiaUtil

    //     const createdObjectByEntity1 = await sut(input)
    //     const createdObjectByEntity2 = await sut(input2)

    //     const compare1 = createdObjectByEntity1.usePerformPromocaoEntrega
    //     const compare2 = createdObjectByEntity2.usePerformPromocaoEntrega
    //     const response = compare1 === compare2

    //     expect(response).toBeTruthy()
    //     expect(response).not.toBeFalsy()
    // })


})

