/* eslint-disable @typescript-eslint/no-unused-vars */
import { ArgsCreateProduct } from "@src/core/product/entity/product.contract"
// import CreateProductController from "@src/entitys/product/cases/create/create_product.controller"
import { seedProductOne } from "@src/core/product/_data/seeds/seedsProducts"

import { describe, it, expect } from "vitest"

describe.todo("Product Entity >> Sucess", () => {
  // const input: ArgsCreateProduct = seedProductOne
  // const sut = CreateProductController

  // it(`deve criar um produto.`, async () => {
  //   const action = await sut.execute(input)
  //   // console.log('VIA CORE >>', action)

  //   expect(action).toHaveProperty('nomeProduto', 'nome produto one')
  // })

})
