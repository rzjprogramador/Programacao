using funcao;
using recursivas;
using armazenadoresDeDados;

namespace principal;

public class Program {
  public static void Main(string[] args) {
    // ArrayFixo.Execute();
    // Lista.VerLista();
    Console.WriteLine(Lista.VerLista());
    // ArrayFixo.LerArray();
    // Recursiva.LerArquivo(1);
    // Console.WriteLine(Recursiva.LerArquivo());
    // Console.WriteLine(ClasseFuncao.FuncaoSomaDeInteiros());
  }
}

/*
o @ arroba antes da string é para escapar as barras e nao dar incompatibilidade.
*/